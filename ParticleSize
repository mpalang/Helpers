L=[1,2,3,4,5,6,7] # Positions of Maxima

def Calcd(l1,l2):
    l1=float(l1)
    l2=float(l2)
    n=l2/(l2-l1)
    d=n*l2
    return d

D=[] #Diameter in 1E-6m
for n in range(len(L)-1):
    D.append(Calcd(L[n],L[n+1])/1000)

print(D)

